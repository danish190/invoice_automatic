import 'package:flutter/material.dart';

class CustomAlertPopup extends StatefulWidget {
  const CustomAlertPopup({super.key});

  @override
  State<CustomAlertPopup> createState() => _CustomAlertPopupState();
}

class _CustomAlertPopupState extends State<CustomAlertPopup> {
  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
